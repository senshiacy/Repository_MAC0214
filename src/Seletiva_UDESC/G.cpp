#include <bits/stdc++.h>

using namespace std;

void solve() {
    int P;
    cin >> P;
    cout << 100 / (__gcd(100, P)) << endl;
}

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    solve();
}